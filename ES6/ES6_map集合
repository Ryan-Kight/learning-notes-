map集合：
map 集合专门用于存储多个键值对数据

若是一个 个体 由 一个 完整的 组合 组成 那么用 对象来包装
可以随意增删的内容可以用map集合

如何创建map
new Map() 创建一个空map
new Map(iterable) 创建一个具有初始化内容的map e.g:[["a",3],["b",4],["c",5]];
将一个可迭代对象，放入map集合中需要符合一定的规范使得map读懂，调用方法可以读里面的键值内容

map方法
.size 只读属性，获取map对象中键的数量
set(键,值) 设置一个键值对   存在则修改，不存在则添加
get(键) 输入键，得到值
has(键) 输入键，判断是否存在
delete(键) 删除指定的键
clear() 清空map集合

如何转换为数组？
const arr = [...map]
遍历
使用for of 循环，
使用forEach 参数：1.值 2.键 3.map本身
